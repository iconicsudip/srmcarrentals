"use client";

import { BookingsList } from "@/components/admin/bookings/bookings-list";

export default function AllBookingsPage() {
  return <BookingsList title="All Bookings" description="Every booking across every status." />;
}
